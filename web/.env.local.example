# Base URL of the FastAPI backend (no trailing slash).
# Prefer http://localhost:8000 if the UI is http://localhost:3000 (same hostname
# family as the browser “site”; avoids some strict CORS / cross-site quirks vs 127.0.0.1).
NEXT_PUBLIC_API_URL=http://localhost:8000
